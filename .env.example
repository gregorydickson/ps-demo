# Google AI API Key (for Gemini models and embeddings)
GOOGLE_API_KEY=your_google_api_key

# LlamaParse API Key (for PDF parsing)
LLAMA_CLOUD_API_KEY=your_llamaparse_key

# FalkorDB Configuration (graph database)
FALKORDB_HOST=localhost
FALKORDB_PORT=6379

# Redis Configuration (for cost tracking, uses separate instance)
REDIS_URL=redis://localhost:6380

# For testing with FalkorDB (when local Redis is on 6379)
FALKORDB_TEST_PORT=6381

# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000
